<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartBench.Base"
        android:usesCleartextTraffic="true">

        <!-- Login — ponto de entrada -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartBench.Base"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.RedefinirSenhaActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartBench.Base"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="smartbench" android:host="reset-senha" />
            </intent-filter>
        </activity>

        <!-- Host Activities por perfil -->
        <activity
            android:name=".ui.admin.AdminHostActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartBench.Admin"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.supervisor.SupervisorHostActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartBench.Supervisor"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.mecanico.MecanicoHostActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartBench.Mecanico"
            android:windowSoftInputMode="adjustResize" />

        <!-- FileProvider para compartilhar arquivos CSV -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
